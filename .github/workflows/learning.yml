name: learn-github-actions
on: [ push ]
jobs:
  check-bats-version:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install
      - run: npm run dev-build
      - run: scp -r ./public -i ${{secrets.ID_RSA_PUB}} root@37.228.117.174:~/public

